import type { Config } from "tailwindcss";

const config: Config = {
    darkMode: ["class"],

    // Not required in V4, but useful if you want to enforce CSS scanning only from globals.css
    content: [],

    plugins: [],
};

export default config;
